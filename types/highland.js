/* @flow */

/* Highland stream */
export type highlandStream = {
  map: Function,
  through: Function,
  flatMap: Function,
  doto: Function,
  flatten: Function,
  stopOnError: Function,
  each: Function,
  done: Function,
}
